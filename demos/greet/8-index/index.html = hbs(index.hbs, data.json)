<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
  </head>
  <body>
    <h1>People</h1>
    <ul>
      {{#each this}}
        <li><a href="{{@key}}.html">{{@key}}</a></li>
      {{/each}}
    </ul>
  </body>
</html>
